@{
    ViewData["Title"] = "Inicio";
}

<div class="container mt-5">
    <div class="text-center mb-5">
        <h1 class="display-4">🏛️ Bienvenido a JustiSafe</h1>
        <p class="lead text-muted">Sistema Seguro de Anonimización Judicial</p>

        @if (User.Identity?.IsAuthenticated == true)
        {
            <div class="alert alert-success d-inline-block">
                👋 Sesión activa para: <strong>@User.Identity.Name</strong>
            </div>
        }
    </div>

    <div class="row justify-content-center">

        <div class="col-md-5 mb-4">
            <div class="card h-100 shadow-sm border-primary">
                <div class="card-body text-center">
                    <div class="display-1 mb-3">📂</div>
                    <h3 class="card-title text-primary">Gestión de Casos</h3>
                    <p class="card-text">
                        Acceda a sus expedientes asignados y actualice sentencias.
                    </p>
                    <div class="d-grid gap-2">
                        <a asp-controller="Cases" asp-action="Index" class="btn btn-primary btn-lg">
                            Ver casos asignados
                        </a>
                    </div>
                </div>
            </div>
        </div>

        <div class="col-md-5 mb-4">
            <div class="card h-100 shadow-sm border-success">
                <div class="card-body text-center">
                    <div class="display-1 mb-3">💬</div>
                    <h3 class="card-title text-success">Soporte Anónimo</h3>
                    <p class="card-text">
                        Canal seguro y encriptado para asistencia técnica.
                    </p>
                    <div class="d-grid">
                        <a asp-controller="Chat" asp-action="Index" class="btn btn-success btn-lg mt-auto">
                            Abrir Chat en Vivo
                        </a>
                    </div>
                </div>
            </div>
        </div>

    </div>
</div>